/**
 * Parser versioning — bump these on every release.
 * Included in every parseResumeFile() output under the `meta` key.
 */
export const PARSER_VERSION     = '3.0.0'
export const DICTIONARY_VERSION = '2026.08'
